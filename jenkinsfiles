pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // Checkout the source code from GitHub
                checkout scm
            }
        }
        stage('Build') {
            steps {
                // Install dependencies and build React.js project
                sh 'npm install'
                sh 'npm run build'
            }
        }
    }
}
